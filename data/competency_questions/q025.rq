# category: "Works and Expressions"
# question: "Find musical works that are specifically categorized by their geographical or regional origin."
# query:
SELECT * WHERE {
   ?expression rdfs:label ?label ;
        mus:U65_has_geographical_context []
}
