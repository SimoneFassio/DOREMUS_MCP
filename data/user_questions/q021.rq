# category: "Recordings"
# split: "easy"
# origin: user_question
# question: "Give me recordings of the work Bolero."
# used_tools: ["build_query", "apply_filter", "add_select", "execute_query"]
# query:
SELECT DISTINCT ?track ?workTitle
WHERE {
  ?track a mus:M24_Track ;
         mus:U51_is_partial_or_full_recording_of ?performance .
  ?performance mus:U54_is_performed_expression_of ?work .
  ?work rdfs:label ?workTitle .
  FILTER ( REGEX(?workTitle, 'Bol[eé]ro', 'i') )
}

# workflow
# build_query(question="Give me recordings of the work Bolero.", template="track")
# apply_filter(query_id="q21", base_variable="track", template="track", filters={"work_title": "Bolero"})
# execute_query(query_id="q21")
